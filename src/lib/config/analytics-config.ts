// This file contains configuration for analytics integrations

// Vercel Analytics configuration
export const vercelAnalyticsConfig = {
    // Set to true to enable Vercel Analytics integration
    enabled: true,

    // Instructions for setting up Vercel Analytics API access:
    // 1. Go to your Vercel dashboard
    // 2. Navigate to Settings > Tokens
    // 3. Create a new token with Analytics permissions
    // 4. Add the token to your environment variables as VERCEL_ANALYTICS_TOKEN
    // 5. Add your project ID as VERCEL_PROJECT_ID (found in Project Settings)
}
